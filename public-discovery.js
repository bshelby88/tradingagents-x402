"use strict";
function registerPublicDiscovery(app,config){const baseUrl=config.baseUrl.replace(/\/$/,"");const links={manifest:`${baseUrl}/.well-known/x402.json`,openapi:`${baseUrl}/openapi.json`,health:`${baseUrl}/health`,sample:config.sample?`${baseUrl}${config.sample}`:null};app.get("/llms.txt",(_req,res)=>{const lines=[`# ${config.name}`,"",`> ${config.summary}`,"",`- Paid endpoint: ${config.method||"POST"} ${baseUrl}${config.endpoint}`,`- Current configured price: ${config.price} USDC per successful call`,`- Payment network: Base mainnet (eip155:8453), USDC via x402`,`- Intended users: ${config.audience}`,`- x402 manifest: ${links.manifest}`,`- OpenAPI: ${links.openapi}`,`- Health: ${links.health}`];if(links.sample)lines.push(`- Free sample: ${links.sample}`);if(config.disclaimer)lines.push(`- Note: ${config.disclaimer}`);res.type("text/plain").send(`${lines.join("\n")}\n`)});app.get("/pricing.md",(_req,res)=>{const sampleLine=links.sample?`\n- Free evaluation: [sample response](${links.sample})`:"";res.type("text/markdown").send(`# Pricing — ${config.name}\n\n- Price: **${config.price} USDC per successful call**\n- Billing: pay per request; no account or subscription\n- Network: Base mainnet (eip155:8453)\n- Paid endpoint: \`${config.method||"POST"} ${config.endpoint}\`\n- Live payment requirements: [x402 manifest](${links.manifest})${sampleLine}\n\nThe live x402 payment challenge is authoritative if the configured price changes.\n`)});if(config.homepage!==false)app.get("/",(_req,res)=>res.type("html").send(""));}module.exports={registerPublicDiscovery};
